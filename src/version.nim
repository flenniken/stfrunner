## The current stfrunner version number.

const stfrunnerVersion* = "0.1.1"
  ## The statictea version number. It follows a subset of Semantic
  ## Versioning; it always uses three components and each uses 1 to 3
  ## ascii digits.
